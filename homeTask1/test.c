#include <stdio.h>
#include "bn.c"

int main(){
    printf("\n---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------\n");

    bn* a = bn_new();
    bn* b = bn_new();
    bn_init_string(a, "12");
    bn_init_string(b, "-123");

    bn* c = bn_init(a);
    bn_div_to(c, b);

    bn_print(a);
    bn_print(b);

    if(bn_cmp(a, c) == 0)
        printf("(unchanged)");
    bn_print(c);

    printf("--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- \n\n");
    bn_delete(a);
    bn_delete(b);
    bn_delete(c);

    return 0;
}